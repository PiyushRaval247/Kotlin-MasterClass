# Lesson 13: Practice Exercises

## Easy
Create a data class `Book` with properties `title`, `author`, and `price`.

## Medium
Create a copy of a `User` data class, modifying only the email field using the `copy()` function.

## Hard
Show destructuring declarations using a data class holding 3 coordinate properties (X, Y, Z).
