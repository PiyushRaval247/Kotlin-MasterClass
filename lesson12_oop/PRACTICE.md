# Lesson 12: Practice Exercises

## Easy
Create a class `Car` with properties `make`, `model`, and a method `drive()`.

## Medium
Implement a class hierarchy where `Vehicle` is abstract and `Car` and `Bike` inherit from it.

## Hard
Create an interface `PaymentGateway` and implement it in classes `Paypal` and `Stripe`. Show polymorphic behavior.
