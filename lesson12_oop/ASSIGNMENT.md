# Lesson 12: Assignment - School Management System

## Real-world Task
Model a school. Create a base class `Person` and subclasses `Student` and `Teacher`. Override a `displayInfo()` function to show polymorphic output.
