# Lesson 20: Practice Exercises

## Easy
Wrap a string-to-integer conversion in a try-catch to prevent a NumberFormatException.

## Medium
Create a custom exception class `InvalidAgeException`. Write a function that throws it if age is < 0.

## Hard
Demonstrate how the `finally` block executes even if a function returns early.
