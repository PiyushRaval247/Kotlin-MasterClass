# Lesson 15: Assignment - ATM Transaction Status

## Real-world Task
Create an ATM status handler. Declare a sealed class `TransactionResult` with `Success` (containing new balance), `Failure` (containing error message), and `Processing`. Handle transactions dynamically.
