# Lesson 15: Practice Exercises

## Easy
Create a sealed class `NetworkState` with subclasses `Success`, `Error`, and `Loading`.

## Medium
Evaluate a `NetworkState` sealed class within a `when` expression and return appropriate feedback.

## Hard
Model a complex file analyzer system using sealed classes representing formats: `TextFile`, `ImageFile`, `VideoFile` with metadata.
