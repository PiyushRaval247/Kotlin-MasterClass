# Lesson 02: Assignment - System Config Details

## Real-world Task
Create a configuration simulator. Declare constants for system limits (e.g. `MAX_USERS = 100`) and a variable tracking current active sessions. Output a report before and after adding a user.
