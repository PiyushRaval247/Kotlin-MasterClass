# Lesson 14: Practice Exercises

## Easy
Create an enum class `LogLevel` with levels: INFO, WARNING, ERROR.

## Medium
Create an enum `PaymentStatus` with an abstract method that returns a human-readable description.

## Hard
Combine an enum class representing weekdays with a `when` block evaluating if a weekday is a working day or weekend.
