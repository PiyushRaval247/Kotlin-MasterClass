# Lesson 22: Practice Exercises

## Easy
Use `runBlocking` and `launch` to run a coroutine that prints a message after a delay.

## Medium
Perform two simulated API calls concurrently using `async`/`await` and print the combined results.

## Hard
Create a coroutine that periodically prints progress every 500ms and allows cancelling it.
