# Mini Project 1: Calculator

A menu-driven arithmetic calculator that reads inputs and calculates results. Stores operation history in-memory.

## Requirements
* Double numeric input support
* Modulo, add, subtract, multiply, divide
* Catch division by zero and number formatting errors
* History log display
