# Lesson 18: Practice Exercises

## Easy
Use `let` to print a message only if a variable is not null.

## Medium
Implement a custom higher-order function `calculate` that applies an operation to two numbers.

## Hard
Chain `apply`, `also`, and `let` to build a student object, print status notifications, and extract an ID.
